apiVersion: v1
kind: Service
metadata:
  name: packagemanage-service
spec:
  selector:
    app: package-manage
  ports:
    - name: package-manage-read-port
      port: 80
      targetPort: 8090
---
apiVersion: v1
kind: Service
metadata:
  name: package-manage-web
spec:
  selector:
    app: package-manage-web
  ports:
    - name: package-manage-web-read-port
      port: 8080
      targetPort: 8080

